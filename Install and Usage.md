Install Py Environment to run this in a virtual environment

python3 -m venv Stewartenv
pip install numpy
pip install scipy
pip install matplotlib

#alternately use this single line
pip install numpy scipy matplotlib